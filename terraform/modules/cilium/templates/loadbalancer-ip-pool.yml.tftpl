apiVersion: "cilium.io/v2alpha1"
kind: CiliumLoadBalancerIPPool
metadata:
  name: external-lb-pool
spec:
  blocks:
%{ for item in lb_ip_pool_cidrs ~}
    - cidr: ${item.cidr}
      start: ${item.start}
      stop: ${item.stop}
%{ endfor ~}
